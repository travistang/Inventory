import * as Qty from 'js-quantities'

// export const recognizedUnits = "length,mass,volume"
//   .split(',')
//   .map(Qty.getUnits.bind(Qty))
//   .reduce((res, units) => ([...res, ...units]))

export const recognizedUnits = "kg,g,mg,mL,L,cm,m".split(',')
